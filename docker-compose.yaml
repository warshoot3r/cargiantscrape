version: '3'

services:
  cargiantscrape:
    build: 
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    volumes:
      - ./:/app/used_cars.db
      - ./modules:/app/modules/
      - ./credentials.py:/app/credentials.py
